<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.hht.myfi">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Income.IncomeActivity" />
        <activity android:name=".Expense.ExpenseActivity" />
        <activity android:name=".Income.EditIncomeActivity" />
        <activity android:name=".Expense.EditExpenseActivity" />
        <activity android:name=".Debt.DebtActivity" />
        <activity android:name=".Debt.EditDebtActivity" />
        <activity android:name=".Loan.LoanActivity" />
        <activity android:name=".Loan.EditLoanActivity" />
        <activity android:name=".ManageIncomeExpenseCategory.ManageIncomeExpenseCategoryActivity" />
    </application>

</manifest>